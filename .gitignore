# Node / frontend
node_modules/
dist/

# VS Code "Local History" extension's auto-saved snapshots -- editor
# scratch data, not source.
.history/

# C++ backend build output (regenerated by CMake)
patient_dashboard/backend/build/

# Runtime data written by patient_server.exe (encrypted patient store,
# accounts, appointments, etc.) -- not source, and holds whatever data
# whoever runs the server locally created.
patient_dashboard/backend/data/
